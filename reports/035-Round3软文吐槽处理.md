# 035 · 体验官 Round 3（软文）：一条无出处的数字，和我们自己破的规矩（2026-09-14）

## 背景

体验官第三轮把刀口转向外宣：抓了知乎账号「陈灏」名下 4 篇 OpenHire 相关内容，
与仓库 021/023/024/029/030/031/032 号报告互证，另核了 GitHub API。
他的总评是「内容比预想的能打」，但下面每条都按「优等生怎么变满分」的标准开刀。

**报告原件：** `drafts/tester/`（.md 附件，正文仅一句「报告见附件」——按上一轮约定执行了）

## P0-① 全文最硬的数字没有出处（最严重，已修）

幽灵岗位长文里写着：「多家招聘平台的抽样数据显示，线上招聘信息里大概有五分之一是幽灵岗位。」

**在一篇以「我不是猜的，是查过的」立信的文章里，唯一那个硬数字是凭印象写的。**

没有去补来源，**换成我们自己能复核的数**，并在文中挂更正说明：

```
挂超 90 天  约 43%      挂超 180 天  约 22%
挂超一年    约 9%       中位          75 天
```

「超过半年」那 22% 和坊间「五分之一」数量级对得上，但**这是我们自己量的，读者能复现**。
同段落把边界写死：**量得到「挂了多久」，量不到「对方还想不想招」**，三种解释（没打算招 /
真招不到 / 常青岗）数据上分不开。

## P0-② 示例指令撞墙 → 发 0.6.0

具身文结尾让读者问 AI「帮我查一下小鹏现在在招哪些智驾岗」。
体验官指出：**产品答不了**（`search_jobs` 无 company 参数，他在同日产品报告里列为 S1）。

根因当天已修但没发出去。**于是发了 0.6.0**（PyPI 已上线，v0.6.0 tag 已推触发 registry 自动发布）。
内容承诺的能力，发布版必须真有。

## P1-② 破折号 11 处：规矩是我们自己定的，也是我们自己破的

`reports/023` 明确定过「零破折号」的行文规矩。Round 3 实测：v0.2 文 3 处、幽灵文 2 处、
具身文 6 处、BOSS 回答 0 处，**共 11 处**。

三篇逐处按语义改（不做全局替换，破折号该换成逗号、句号还是冒号要看上下文），**现已全部归零**。

## P1-① numbers.json：根治数字漂移

所有文章写「139 家」，`companies` 表 140 行，**连续两轮被抓**。

这从来不是数错了，**是两个不同的问题共用了一个标签**。新增 `ohp numbers`：

```json
{ "companies_in_index": 140, "employers_with_live_postings": 139,
  "live_postings": 15966, "median_days_open": 75,
  "pct_open_over_90d": 43.3, "pct_open_over_180d": 22.3, "pct_open_over_365d": 8.8 }
```

**每个字段名都说清它数的是什么。** 有测试断言两个计数都存在、且后者不大于前者。

**它当场抓到我自己一小时前的漏**：文章写 22.4%，管道算 22.3%。于是把文章改成取整 +
标注实测日期，周更刷新就不会让它回溯变错。

`CLAUDE.md` 新增两条硬规矩：数字只准引用 `docs/numbers.json`、零破折号。

## P0-③ 人设收束（领导批准）

一个月三张名片：「我不是程序员，是个 PM」/「我做过几年猎头 FA」/「我做过几年深科技猎头」。

**批准的方案**：主叙事 =「深科技猎头，覆盖智驾与具身」；
「用 AI 结对写代码」只作**方法说明**，不作身份。

- v0.2 文：「我不是程序员，是个 PM」→「我是做深科技猎头的，不是程序员，代码全程用 Claude Code 结对写出来」
- 幽灵文：「我做过几年猎头 FA」→「我做过几年深科技猎头，主要覆盖智驾和具身」
- 具身文：本来就是主叙事，未动
- README 英文版同步
- 规则写进 `CLAUDE.md`

**BOSS 回答那篇没改。** 它写的是「猎头 FA」，与主叙事同职业、非冲突项
（体验官指的真冲突是 PM 那张，已拔掉）；而知乎**回答没有草稿 API**，只能走 UI 编辑器，
记忆 `zhihu-editor-automation` 明确记着编辑器拒绝自动化注入。
**这是我们流量最好的一篇（14 赞同），不为 5 个字的标签差异冒险。**

## 其余已修

| 项 | 处理 |
|---|---|
| v0.2 文是 release notes 顺序反了 | 开头补「一句话说清这次更新对你的意义」的读者视角段 |
| 「5 家扩到 34 家」读者算不明白 | 改为「这两个赛道覆盖的公司数从 5 家扩到 34 家」 |
| 幽灵文只教查发布日、没教查重挂 | 补「怎么查重挂」的两个笨办法（存链接隔周再看 / 搜聚合站收录次数） |
| 具身文口径三连问 | 补脚注：30 家全量非抽样、表只列 25 家、0% 是四舍五入不足 0.5% |

## 核实而非采信的三件事

1. **「op enhire」** —— 我方文案**零命中**，确系他抓取器对 URL 的伪影，不是双料问题。
2. **awesome-mcp-servers 收录** —— 他替我们核了 PR #13379 于 9/7 合并，属实。
3. **「回答分发是文章的 2.25 倍」** —— **这是我们 024 号报告自己量出来的结论**，
   而我昨天从知乎后台「重新发现」了一遍并当成新结论汇报。
   **已知最优解，执行和记忆都没跟上**，这条我认。

## 验收

- 301 tests 全绿
- 三篇文章逐篇服务端核验：破折号 0、更正块在位、表格完整、人设已统一
- 0.6.0 已上 PyPI（`latest: 0.6.0`），tag 已推

## 追查结果：那篇「7 月首发帖」根本不存在（本单最重的一条）

体验官把它标成「未取得 —— 搜索未收录独立页、主页被反爬拦截」。
**「未取得」不等于「不存在」，我差点也这么记进台账。** 领导说没印象，让我直接去查。

三条路全查遍（用创作者 API，不靠搜索）：

| 形态 | 范围 | 7 月的 OpenHire 内容 |
|---|---|---|
| 文章 | 全量，**回溯到 2018 年** | 7 月仅 4 篇：CVPR / ICCV / 自驾人才流向 / 五层奶酪。**零** |
| 想法 | 2026 全年 | 最早的 OpenHire 想法是 **9/02**；7/31 那条讲论文下载 prompt。**零** |
| 回答 | 6 月—8 月中 | 具身智能 / vibe coding / ICCV / GPT5.6 / 自驾人才 / 命运 / 三观。**零** |

**我们自己的台账也对得上**：`reports/` 里最早提到知乎的是 023 号（9/08 幽灵岗位长文）。
7 月没有任何知乎发布记录。

**所以 v0.2 文开头那句是已发布的假话**：

> 「7 月发过 OpenHire 的首发介绍（一个让 AI 助手直连雇主 ATS 一手职位数据的开源 MCP server）。」

7 月发的是 **GitHub 上的 v0.1 版本**（Release v0.1.0，7/14），不是知乎上的介绍文。
**这句是我写的，我把两件事混了**，而它公开挂了 12 天。

**已改**：文章开头换成更正说明（写明查了什么、结论是它从来没发过），
并补上一段「OpenHire 是什么」——因为原文是靠那句不存在的首发帖来交代背景的，
删掉它之后新读者会断片。服务端已核验上线。

**讽刺之处**：这正是本轮在修的那一类问题——**一个凭印象写下、没人核对的陈述**。
体验官抓了「五分之一」，没抓到这条，因为他没法证伪一篇他抓不到的文章。
**可证伪性掌握在我们自己手里，不在外部评审手里。**

## 未做

- **9/02 的「想法」仍写着「能让 Claude 直接搜了」**（文章标题已去 Claude 化）。
  知乎想法基本不可编辑，且是时间流内容、无检索价值，判定不值得折腾，记录在案
- **P2 素材库轮换**（三套安装话术、比喻编号管理）—— 承认问题成立，但优先级低于分发
