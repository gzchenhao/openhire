# 008 · 发布执行记录（GitHub + PyPI 上线）

- **日期：** 2026-07-14
- **状态：** ✅ **已上线**。GitHub 仓库 + v0.1.0 Release + 快照资产 + PyPI 包，全部就绪，端到端实测通过。
- **测试基线：** 118 passed（发布代码未改动逻辑）。

---

## 成果链接
- **GitHub：** https://github.com/gzchenhao/openhire （Public，main 分支，72 文件）
- **Release：** https://github.com/gzchenhao/openhire/releases/tag/v0.1.0 （资产 `openhire-index.db.gz` 13.2MB）
- **PyPI：** https://pypi.org/project/openhire/0.1.0/ （`pipx install openhire` 全网可装）

---

## 逐步执行记录

| 步 | 操作 | 结果 |
|---|---|---|
| 0 | 占位 `OWNER`/`openhire/openhire` → `gzchenhao`（pyproject urls + config.SNAPSHOT_URL）；重建包 | ✅ twine check PASSED |
| 1 | `winget install GitHub.cli` | ✅ gh 2.96.0（无需 UAC） |
| 2 | 用户 `gh auth login --web` 授权 | ✅ Logged in as gzchenhao（scope: repo, workflow） |
| 3 | `git init` + 首次提交 + `git push -u origin main` | ✅ commit `2d361db`，72 文件；**密钥零泄漏**（.env/.pypirc/*.db 均被 .gitignore 挡下，暂存区核对为空） |
| 4 | `ohp snapshot-build` + `gh release create v0.1.0` 传资产 | ✅ 快照零用户态校验通过；资产下载 URL 与 `config.SNAPSHOT_URL` 完全一致（HTTP 200） |
| 5 | `python -m build`（干净目录，不含快照）+ `twine upload` | ✅ 上传 wheel+sdist；token 全程只在 `%USERPROFILE%\.pypirc`，未入代码/git |
| 6 | 全新 venv `pip install openhire`（真 PyPI）+ `ohp bootstrap` + `ohp search` | ✅ 见下 |

### 第 6 步端到端证据（从 PyPI 装的 ohp 实跑）
```
$ ohp version → openhire 0.1.0
$ ohp bootstrap
  ① 下载快照：https://github.com/gzchenhao/openhire/releases/download/v0.1.0/openhire-index.db.gz
  ✓ 快照就绪 · 公司 96 · 职位 11825 · 数据截至 2026-07-10（龄 4 天前）
  ② 增量刷新 → ✓ 新增 457 · 更新 594 · 下线 581 · 公司 96
$ ohp search --required-skills rust,k8s --remote --role-family engineering
  ✓ MongoDB · Security Software Engineer … datePosted=2026-03-24 · days_open=112 · country_locked [US]
```
证明：陌生用户 `pipx install openhire` → `ohp bootstrap` 即可零配置拿到最新索引并直接搜索。

---

## 隐私 / 安全红线执行情况
- **代码库零密钥**：`.env`（含真实 DEEPSEEK_API_KEY）、`.pypirc`、`*.db`、`*.db.gz` 全部被 `.gitignore` 排除；push 前逐一核对暂存区，无泄漏。
- **快照零用户态**：`snapshot-build` 构建时强制校验 watches/applications=0；发布的资产不含任何用户数据。
- **token 隔离**：PyPI token 仅在本机 `.pypirc`；GitHub 用 `gh` keyring 凭据，均未经手代码/git。

---

## 仍待办（需用户点头）
1. **提交 Smithery（MCP 目录）** —— `smithery.yaml` 已在仓库；提交前把目录页要填字段整理给用户确认，**不擅自提交**（沿用 006 约定）。
2. **快照每周刷新** —— 按 `docs/maintainer-snapshot-refresh.md`，每周 `ohp snapshot-build` + 更新 Release 资产（保持文件名不变）。
3. （可选）PyPI 上传成功后，可在 PyPI 账户把 token 改为**项目范围**的新 token、删掉临时的账户级 token。

---

## 结论
OpenHire v0.1「哨兵」**已公开发布**：GitHub 源码 + Release 快照 + PyPI 包三者齐备并端到端验证。M1–M4 全部完成，三条隐私红线全程未破，另有快照零用户态的自动化守护。下一步仅剩 Smithery 目录提交（待用户点头）。
